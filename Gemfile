source 'https://rubygems.org'

gem 'activesupport', '4.2.1'

# Add dependencies to develop ansible_model here
# Include everything needed to run rake, tests, features, etc.
group :development do
  gem 'bundler', '~> 1.0'
  gem 'jeweler', '~> 2.0.1'

  # Use cucumber & rspec for tests
  gem 'shoulda', '>= 0'
  gem 'cucumber', '~> 2.0'
  gem 'rspec', '~> 3.2'

  # Code coverage using simplecov
  gem 'simplecov', '>= 0'

  # Code style checking
  gem 'rubocop'

  # Documentation
  gem 'rdoc', '~> 4.2.0'
  gem 'yard'
end
